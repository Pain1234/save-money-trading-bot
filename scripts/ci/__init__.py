"""CI helper scripts for the fast/full GitHub Actions split (Stage 2).

This package intentionally has no runtime dependencies outside the Python
standard library so it can run in the earliest, cheapest CI job (``plan``)
before any project dependencies are installed.
"""
