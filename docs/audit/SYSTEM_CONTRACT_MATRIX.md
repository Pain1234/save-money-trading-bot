# System Contract Matrix

Audit issue: [#371](https://github.com/Pain1234/save-money-trading-bot/issues/371)
Frozen code: `7b78eb9996eb16e6d2ec6a00c2e1908c518682d9`

This matrix keeps five evidence layers separate: documented contract, implementation,
test evidence, observed deployment, and not verified. A green test is not treated as
runtime evidence. Status values are limited to the vocabulary required by Issue #371.

| Bereich | Sollverhalten | Quelle | Implementierung | Testevidenz | Runtime-Evidenz | Status |
|---|---|---|---|---|---|---|
| Execution ownership | Exactly one process may mutate a paper account. | `docs/ARCHITECTURE.md`; ADR-018; paper runbooks | PostgreSQL advisory lock guards worker startup; shutdown releases it before the final `STOPPED` write. | Single-process helpers are green; no valid two-process PostgreSQL audit run. | Owner, lock holder and replica count unavailable. | PARTIALLY_VERIFIED |
| Strategy-to-order boundary | Strategy emits signals/intents, never direct exchange orders. | strategy and paper contracts | Strategy Engine returns intents; Paper lifecycle owns simulated fills. No wallet/live adapter exists. | Strategy/Paper offline suites green. | Not observed in deployed worker. | VERIFIED |
| Research/execution separation | Research must not automatically promote to Paper/Live. | public/private boundary; P4/P5 docs | Research jobs and Paper scheduler are separate; no automatic promotion call found. | Research acceptance and API suites green. | Private roots and deployed environment unavailable. | PARTIALLY_VERIFIED |
| Human gates | P5 and later phase actions require explicit human decisions. | `ROADMAP.md`; P5 runbook; ADRs | Gate/decision records exist; no audit action opened P5. | Gate negative tests green. | Private ACLs and holdout storage not inspected. | PARTIALLY_VERIFIED |
| Public live boundary | No wallet signing, live orders or live secrets in Public Core. | `AGENTS.md`; architecture and security docs | Repository-wide code/secret tracing found no signing or live-order implementation. | Live tests were intentionally not run. | Production secrets/config not visible. | PARTIALLY_VERIFIED |
| Dataset identity | Run binds Dataset ID, manifest/hash and code/config identity. | market-data and Research identity contracts | Normal path binds those values, but PostgreSQL raw-ID collision handling can silently preserve a different raw hash. | Positive identity tests green; negative raw-ID conflict is missing. | Real catalog not inspected. | CONTRADICTED |
| Candle integrity | Duplicates, gaps, ordering, UTC and open candles are rejected. | `docs/market-data-contract.md` | Validators and aggregation enforce the normal contract. Production-style Paper higher-timeframe consumption can bypass daily/native conflict comparison. | 172 Market Data tests plus synthetic negatives; Postgres teardown became invalid. | Runtime dataset unavailable. | PARTIALLY_VERIFIED |
| Instrument identity | BTC, ETH and SOL remain consistent across supported services. | ADR instrument/universe contracts | Canonical symbol mapping exists; system is long-only for Strategy V1. | Symbol/constraint suites green. | Deployed payloads unavailable. | VERIFIED |
| Strategy V1 parameters | Only frozen parameters/defaults may execute. | `docs/strategy-specification.md`; parameter inventory | Defaults match, but unknown Research keys enter identity and are silently ignored by execution. | Positive defaults green; negative probe reproduced silent ignore. | Real public/private run config not opened. | CONTRADICTED |
| Strategy V1 exits | Monthly-regime exit, stops/targets/trailing behavior match the frozen contract. | strategy specification | Monthly-regime exit reason exists but no executable intent/consumer path exists. | No matching executable regression found. | Not observed. | NOT_IMPLEMENTED |
| Determinism | Same valid input produces same signal and identity. | strategy/research reproducibility docs | Core computations are deterministic; ignored unknown keys break identity-to-effective-config equivalence. | Reproducibility suites green for declared inputs. | Not observed. | PARTIALLY_VERIFIED |
| Backtest/Paper parity | Same data/config yields contractually explained matching signals and economics. | `BACKTESTER_PAPER_PARITY.md` | Both call Strategy Engine, but higher-timeframe provenance and gap-fill economics differ. | Shared synthetic parity tests green; adversarial cases contradict parity. | No deployed parity run. | CONTRADICTED |
| Risk sizing/limits | Frozen trade, portfolio, leverage, count and stop-distance limits gate entry. | risk specification and inventory | Risk Engine validates documented numeric boundaries and uses rounded quantity. | 50 Risk tests and wider offline suite green. | Runtime decisions unavailable. | PARTIALLY_VERIFIED |
| Pause/kill/readiness | Any unsafe state blocks new risk. | paper README/orchestrator contract | Production context ignores persisted `runtime.paused`; already scheduled fills do not re-read pause/kill/readiness. | Safe reproductions confirmed both missing gates. | Production worker not exercised. | CONTRADICTED |
| Idempotency/restart | Duplicate command/restart cannot create duplicate position. | paper orchestrator/runbooks | Idempotency keys and DB constraints exist. Startup validates structure but does not consume independent accounting reconciliation. | Offline idempotency green; valid PostgreSQL crash/restart proof unavailable in this audit. | Not observed. | PARTIALLY_VERIFIED |
| Partial fill/cancel/protective orders | Documented lifecycle transitions and stop maintenance exist. | risk/order-lifecycle specification | Enums/schema exist; implemented Paper path performs a single full fill and does not persist protective stop orders/cancel-replace. | No end-to-end partial/cancel regression exists. | Not observed. | NOT_IMPLEMENTED |
| Gap stop economics | Gap stop fills exactly at candle Open under frozen V1. | `docs/risk-specification.md` | Gap reference uses Open, then generic adverse slippage changes the persisted fill. | Pure scenario reproduced the difference. | Not observed. | CONTRADICTED |
| Accounting | Cash, realized/unrealized PnL, fees, funding and equity reconcile independently. | risk/accounting contracts | Closed-trade formulas are coherent; production snapshots omit marks, recovery omits independent wallet proof, funding is not checked. | Unit formulas green; valid DB/API/UI chain not completed. | Deployed DB/API unavailable. | CONTRADICTED |
| Experiment invalidation | Invalidated evidence can never become valid through fallback/reconstruction. | `docs/research/INVALIDATION.md` | Lookup trusts latest JSONL status and reconstruction can append `complete` despite invalidation sidecar. | Negative temp-directory probe reproduced reactivation. | Real registry not inspected. | CONTRADICTED |
| Scorecard pinning | Scorecards/studies read sealed, pinned evidence only. | Research scorecard/gate contracts | Pin/checksum validation paths exist; actual public provenance chain was not present and private chain was excluded. | Negative pin/artifact suites green. | Runtime/private evidence unavailable. | PARTIALLY_VERIFIED |
| Read-only Monitor | Browser monitor cannot mutate Paper state. | dashboard/read API docs | Dashboard routes proxy GETs; control API is disabled in read-only app. | API and Playwright negative-method tests green. | Public login only observed. | VERIFIED |
| Read-only Research | Research UI/backend is read-only. | UI/read-only claims | UI proxies allow authenticated Research POSTs and combined backend mounts unauthenticated write routes. | Stub Playwright rejects writes, while backend write tests deliberately accept them. | Private-network isolation unavailable. | CONTRADICTED |
| UI semantics | Missing, zero, PASS/VALID/READY/PENDING remain distinct. | product/UI contracts | Most mapping is explicit; validation `n_failed=0` renders unavailable and incident view falls back to normal events. | Unit tests miss those cases. | Authenticated UI unavailable. | CONTRADICTED |
| Artifact access | Only sealed, allow-listed, checksum-verified files are served. | artifact/security docs | Canonicalization, suffix allow-list, checksum and size rules are implemented. | Artifact negative suite green. | Real artifact roots unavailable. | PARTIALLY_VERIFIED |
| API audit redaction | Returned audit payload cannot expose credentials. | security/API contract | Sanitizer covers only selected top-level keys, not nested/alias secrets. | Synthetic nested marker survives sanitizer. | No actual secret-bearing event observed. | CONTRADICTED |
| Deployment reproducibility | Same SHA identifies same Python/Node behavior and running services expose identity. | deployment/DoD docs | Node uses lockfile; Python image installs lower-bound `pyproject` dependencies rather than pinned baseline. Build SHA/digest is not exposed. | Exact-SHA Full CI green; local build green with worktree warning. | Public login observed; service SHAs/digests unavailable. | DEPLOYMENT_DRIFT |
| Recovery fail-closed | Failed startup recovery disables later mutations. | recovery/operations contracts | Research recovery logs failure but keeps subsequent write routes available. | Failure-to-write-gate regression missing. | Occurrence unknown. | CONTRADICTED |
| P5 boundary | Holdout stays sealed; #204/#205 remain blocked; audit runs no P5 action. | public P5 status/calendar/issues | Audit did not open artifacts or invoke P5. Public P5 documents disagree on #251-#254/policy-signoff chronology. | No P5 test or run executed. | Private holdout state intentionally unavailable. | DOCUMENTATION_DRIFT |
| Architecture documentation | Current P3 persistence and migrations are accurately described. | `docs/ARCHITECTURE.md` | Code/migrations implement durable catalog through 011; architecture still describes pre-P3/in-memory state and 001-009. | Market Data code/tests confirm newer state. | N/A | DOCUMENTATION_DRIFT |
| Required static gate | Repository type gate is reproducibly green. | `AGENTS.md`; `pyproject.toml` | Ruff passes; mypy either resolves installed packages without `MYPYPATH`, or reports 35 source errors with it. | Both commands were executed and failed as recorded. | N/A | CONTRADICTED |

## Human decisions required

1. Reconcile the frozen Monthly-regime exit contract with the implemented Strategy V1.
2. Decide whether Research is a write-capable internal plane or a truly read-only backend,
   then define its authentication and network boundary.
3. Decide the authoritative gap-fill price assumption; do not silently change the frozen
   value or the implementation.
4. Reconcile the public P5 execution-status document with the still-pending policy-bind
   document without opening or inspecting the holdout.
